# DCL visual contract

Reference: `design-reference.png`, generated concept 1536 × 1024. This is a visual direction, not a source of calculated facts. No user approval gate was requested.

Tokens: canvas #101719; surface #151e22; rule #344247; ink #e6edf0; muted #a4b2b9; mint #79deba; amber #dfb45e. Geist sans and IBM Plex Mono, locally bundled. Four-pixel corners; restrained outlines; no stock imagery, vendor logos, gradients, or decorative datacenter.

Composition: 56px header; title and concise subtitle; scenario rail; 290px workload rail and flexible workbench; six-mode navigation; code-native isometric topology; three calculated metrics; semantic comparison table; conditional recommendation strip. Mobile: stacked workload disclosure, topology alternative and candidate articles. Buttons and inputs >= 40px where practical.

Above-fold allowed copy: DCL / Deployment Choice Laboratory; Choose where the workload should run.; One workload. Six deployment paths. Understand the trade-offs.; AI Learning System; EN/TR; scenario names; What are you trying to run?; model, precision, context, concurrency, peak, privacy, utilization, horizon; Compare/Cost/Memory/Scale/Decide/Learn; Educational defaults; Performance unknown. Required extra controls and bilingual translations are part of the user's functional brief.

Intentional correctness deviations: replace every mockup number and status with engine output; remove the generated 'High performance' claim; API memory is provider-managed, never 'requires offload'; NVIDIA fails the flagship memory constraint; no fabricated benchmark or availability claim. Recommendation text derives from calculations. Six scenario presets may be represented by a select plus shortcut buttons to avoid overflow. Additional forms use progressive disclosure.

Component families: application shell, workload controls, shared numeric/select inputs, candidate comparison and inspector, SVG architecture, SVG cumulative-cost plot with table alternative, memory segments, preference contribution rows, guided chapter navigation. Icon family: lucide outline, 16px, 1.75 stroke.
